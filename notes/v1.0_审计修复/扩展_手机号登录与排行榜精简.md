# 扩展复盘：手机号登录 + 排行榜精简（去「全局榜」）

| 项 | 内容 |
|---|---|
| 范围 | 注册/登录/忘记密码的账号标识由自由「用户名」改为**11 位手机号**；昵称保留；DB 列 `username→phone`；同步全部文档 |
| 状态 | ✅ 完成、全量编译通过、迁移幂等(连跑 2 次无报错)、TestUserMgmt 15/15、TestDao 全流程通过 |
| 日期 | 2026-07-11 |
| 文件 | model/dao/ui/test 共 9 个源文件 + schema.sql + 新增 migrate_v1.3 + 6 份文档 |

---

## 一、需求与两个关键决策

用户要求：**昵称保留，注册不用用户名、改用 11 位电话**。

### 决策 1：手机号校验放宽为「11 位数字」`\d{11}`（而非严格手机号 `1[3-9]\d{9}`）
最初我按正规手机号写了 `1[3-9]\d{9}`。但用户随后指定 **admin 手机号 = `00000000000`、z 手机号 = `11111111111`**（便于记忆的测试值），这两个都不符合严格号段。
- 若保留严格手机号校验 `1[3-9]\d{9}`：admin(`00000000000`)、z(`11111111111`) 这类记忆值在注册/登录/忘记密码任一处都会被拒——**种子数据和校验规则自相矛盾**。
- 结论：校验必须与种子数据一致。用户要的本质就是「**11 位数字**」，所以放宽到 `phone.matches("\\d{11}")`。这也更贴合最初「11 位电话」的字面要求。

> **教训**：**输入校验规则要和你写进库的种子数据自洽**。种子里的值如果连自己的校验都过不了，就是个埋着的雷。

### 决策 2：admin=`00000000000`、z=`11111111111`，其余种子不变
- `schema.sql` 种子：admin→`00000000000`、player1→`13800000001`、player2→`13800000002`（admin 单独记忆值）。
- 历史注册用户 `z`（非种子、id=5）在**线上库**直接 `UPDATE` 成 `11111111111`（z 不进 schema，只是改活库）。

---

## 二、代码改动（9 个源文件）

标识符改名是**横切关注点**——从模型一路改到文档。改动点（`grep` 找全，避免漏改导致编译失败）：

| 层 | 文件 | 改动 |
|---|---|---|
| model | [`User`](../../src/main/java/com/game/model/User.java) | 字段 `username→phone`、`getUsername/setUsername→getPhone/setPhone`、toString |
| model | [`PasswordResetRequest`](../../src/main/java/com/game/model/PasswordResetRequest.java) | 仅显示用字段 `username→phone`（JOIN 来的，非表列） |
| dao | [`UserDao`](../../src/main/java/com/game/dao/UserDao.java) | `findByName→findByPhone`；`register`/`login`/`listAllUsers`/`map` 全部 `username` 列→`phone` 列；`login(phone,pwd)`、`SELECT ... WHERE phone=?` |
| dao | [`ResetRequestDao`](../../src/main/java/com/game/dao/ResetRequestDao.java) | `requestReset(phone)`（`WHERE phone=?` 查 id）；`listPending` JOIN `u.phone`；`map` 读 `phone` |
| ui | [`LoginFrame`](../../src/main/java/com/game/ui/LoginFrame.java) | 标签/字段/提示「用户名→手机号」；`doLogin`/`doResetRequest` 加 `\d{11}` 校验、`findByPhone`、`requestReset(phone)` |
| ui | [`RegisterFrame`](../../src/main/java/com/game/ui/RegisterFrame.java) | 手机号校验 `\d{11}`；昵称留空时默认「玩家+手机号后4位」（不再用裸手机号当昵称）；`setPhone` |
| ui | [`AdminFrame`](../../src/main/java/com/game/ui/AdminFrame.java) | 用户表/申请表列名「用户名→手机号」；标题与提示用昵称/手机号 |
| ui | [`MainFrame`](../../src/main/java/com/game/ui/MainFrame.java) / [`LeaderboardFrame`](../../src/main/java/com/game/ui/LeaderboardFrame.java) | 显示名回退 `getUsername()→getPhone()`（各 1 行） |
| test | [`TestDao`](../../src/main/java/com/game/TestDao.java) / [`TestUserMgmt`](../../src/main/java/com/game/TestUserMgmt.java) | 测试号生成 `"138"+8位`；`setPhone`/`login(phone)`/`requestReset(phone)` |

> **昵称默认值的小调整**：原来昵称留空默认取用户名；现在用户名变成手机号（一串数字当昵称很难看），改成默认「玩家+手机号后4位」。

---

## 三、数据库迁移（[`migrate_v1.3_phone.sql`](../../sql/migrate_v1.3_phone.sql)）

把 `user.username` 列改名为 `phone`。**幂等**（information_schema 判断旧列是否还在，已迁移则全跳过）。三个步骤都按「第 1 次才执行」护栏：

1. **先把旧列的值改写成 11 位**（`CONCAT('138', LPAD(id-1, 8, '0'))`，admin 再覆盖成 `00000000000`）——**必须先改数据、再缩列宽**，否则 `VARCHAR(50)→VARCHAR(11)` 遇到超长旧值会在严格模式下报错。
2. **重命名列** `CHANGE COLUMN username phone VARCHAR(11) NOT NULL`（列上的唯一索引随列保留）。
3. **唯一索引改名** `uk_username → uk_phone`。

```
-- 幂等开关：旧列还在吗？
SET @has_username = (SELECT COUNT(*) FROM information_schema.columns
  WHERE ... column_name='username');
SET @s1 = IF(@has_username=1, "UPDATE user SET username=CONCAT(...)", 'DO 0');  -- 先改数据
... admin 覆盖成 00000000000 ...
SET @s2 = IF(@has_username=1, "ALTER TABLE user CHANGE username phone VARCHAR(11)...", 'DO 0');  -- 再改名
```

> **`schema.sql`（全新安装）** 已直接建成 `phone VARCHAR(11)` + `uk_phone`，种子 admin=`00000000000`。
> **线上库**：跑了一次 migrate_v1.3（admin→00000000000），再 `UPDATE z→11111111111`；迁移连跑第 2 次无报错（幂等验证通过）。
> **历史迁移顺序**：migrate_v1.1 里有 `WHERE username='admin'`（旧列名），v1.3 之后**勿再重跑 v1.1**；全新机直接用 schema.sql。

---

## 四、验证

- **全量 javac 编译通过**（2 次：改完代码 1 次、放宽校验后 1 次）。
- **迁移幂等**：连执行 2 次 migrate_v1.3，第 2 次无任何报错、数据不变。
- **TestUserMgmt 15/15 PASS**：注册(手机号)→login(手机号)→role=user→改密→requestReset(手机号)/hasPending/去重→approve→deleteUser→admin 不可删，全绿。
- **TestDao 全流程**：注册(手机号)→login→存战绩→排行榜(topN)→我的记录(mine)→错密码返回 null，全过。
- **线上库** `SHOW COLUMNS` 确认 `phone varchar(11) UNI`、`uk_phone` 索引；admin=`00000000000`、z=`11111111111`。

---

## 五、踩坑 / 收获

- **标识符改名是横切关注点 ⭐**：一个字段名牵动 model→DAO→UI→test→schema→迁移→6 份文档。**先用 `grep` 把所有引用找全**再动手，否则漏一处就编译失败或文档说谎。本次用 `getUsername|setUsername|findByName|用户名|username` 多个模式扫了两轮。
- **校验规则要和种子数据自洽**：严格手机号正则会拒掉 admin(`00000000000`)、z(`11111111111`)——种子能过校验是底线。最终放宽到 `\d{11}`，既满足「11 位电话」、又让记忆值合法。
- **幂等的「列改名」迁移套路**：`information_schema` 判断旧列在不在做开关 + **先改数据再缩列宽**（避免超长报错）+ 数据改写只在首次迁移跑（用同一个 `@has_username` 护栏，重跑不覆盖用户改过的手机号）。
- **历史复盘笔记保持原样**：02/03/05/扩展_用户管理/审计修复 等旧笔记里的「用户名」描述不改——它们是**当时的记录**；新改动写**新笔记**（本篇），并在文档里指出「以本篇为准」。只有**面向未来的答辩准备 00 总纲**跟着改了（那是你要照着背的）。

---

## 六、遗留

- 历史阶段复盘笔记（02/03/05/扩展_用户管理）里仍写「用户名」，属历史记录，不改；阅读时以本篇为准。
- [`JDBC与MySQL驱动与依赖管理.md`](../JDBC与MySQL驱动与依赖管理.md) 里的 JDBC 示例仍用 `username`，那是**通用教学示例**（讲 PreparedStatement 概念），非当前 schema 断言，保留。

---

*相关文件：[`model/User.java`](../../src/main/java/com/game/model/User.java)、[`dao/UserDao.java`](../../src/main/java/com/game/dao/UserDao.java)、[`ui/LoginFrame.java`](../../src/main/java/com/game/ui/LoginFrame.java)、[`ui/RegisterFrame.java`](../../src/main/java/com/game/ui/RegisterFrame.java)、[`sql/schema.sql`](../../sql/schema.sql)、[`sql/migrate_v1.3_phone.sql`](../../sql/migrate_v1.3_phone.sql)。*

---

# ② 排行榜精简：去掉「全局榜」

| 项 | 内容 |
|---|---|
| 范围 | 排行榜去掉「全局榜」按钮；`简单榜`/`困难榜` 各管各难度、一律按分数倒序；`我的记录` 改为在当前难度榜上叠加的「只看自己」开关 |
| 状态 | ✅ 完成、编译通过 |
| 日期 | 2026-07-11 |
| 文件 | [`ui/LeaderboardFrame.java`](../../src/main/java/com/game/ui/LeaderboardFrame.java)（**纯 UI 改动，DAO 未动**） |

## 背景：为什么「全局榜」和「困难榜」看着一样

原排行榜是 **2 难度 × 2 范围 = 4 视图**：

- **难度轴**：`简单榜` / `困难榜`（切 `currentDifficulty`）
- **范围轴**：`全局榜`（`mineMode=false`，全部玩家）/ `我的记录`（`mineMode=true`，只看自己）

问题在于：`全局榜` + 困难 =「全部玩家的困难战绩」，和点 `困难榜`（当 `mineMode=false` 时）显示的**一模一样**——两个轴在「全局」这一格重叠了，所以「全局榜」按钮显得多余、还让人困惑（用户的原话：「全局榜和困难榜好像一样吧」）。

## 改动

去掉「全局榜」按钮，UI 简化为 4 个按钮：

- **简单榜** / **困难榜**：点哪个，就看**该难度的全部玩家**、按分数倒序；同时把 `mineMode` 复位成 `false`（退出「我的记录」过滤）。
- **我的记录**：改为**开关**——在当前难度榜上叠加「只看自己」（`mineMode = !mineMode`，再点一次退出）。
- **返回**：关窗。

视图标签：`当前：简单榜` / `当前：困难榜` / `当前：困难榜 · 我的记录`（颜色随难度：简单绿、困难红）。

## 为什么不用动 DAO ⭐

[`RecordDao`](../../src/main/java/com/game/dao/RecordDao.java) 的两个查询**本来**就符合需求，所以这次是**纯 UI 改动**、零 SQL 改动、零迁移：

- `topN(n, difficulty)`：`WHERE difficulty=? ORDER BY score DESC LIMIT ?` —— 按难度过滤 + 按分数倒序，正好是「某难度的全部玩家榜」。
- `mine(userId, difficulty)`：`WHERE user_id=? AND difficulty=? ORDER BY score DESC` —— 「我在某难度的战绩」，正好是「我的记录」。

> 这正体现 **UI 与数据解耦**的好处：DAO 只管「按分数倒序、按难度过滤」的查询能力；界面上按钮怎么组合，是 UI 层的事。改 UI 不碰 DAO、不碰 SQL、不迁移库。

## 文档同步（去掉「全局榜」描述）

面向未来的文档全改成「简单榜/困难榜（按分数倒序）+ 可叠加我的记录」：

- [`README.md`](../../README.md)、[需求规格说明书](../../docs/需求规格说明书.md)（F4 模块）、[详细设计说明书](../../docs/详细设计说明书.md)（排行榜流程图 + 界面表）、[项目计划书](../../docs/项目计划书.md)、[答辩准备 00 总纲](../答辩准备/00_答辩准备总纲_项目通览与学习路线.md)。
- 历史复盘笔记（`05_排行榜`、`02_数据层`、`扩展_难度排行榜`）保持原样——它们记录的是当时的设计。

## 踩坑 / 收获

- **UI 维度重叠是设计陷阱**：两个切换轴（难度 × 范围）一旦有组合产生相同结果（全局 + 困难 ≡ 困难榜），用户就会觉得「这俩怎么一样」。设计多轴切换要想清楚：每个组合是否**信息量不同**，否则就合并掉冗余的那个轴。
- **能用现成查询就别加新查询**：本次零 DAO 改动，因为 `topN` / `mine` 的能力已覆盖需求。加按钮前先看 DAO 有没有现成能支撑的，避免无谓的 SQL 膨胀。
- **「我的记录」做成开关而非单向**：点一次进、再点一次出，比「只能进、要靠点难度按钮才能出」更直觉。

---

*相关文件：[`ui/LeaderboardFrame.java`](../../src/main/java/com/game/ui/LeaderboardFrame.java)、[`dao/RecordDao.java`](../../src/main/java/com/game/dao/RecordDao.java)（未改动，复用 `topN`/`mine`）。*
